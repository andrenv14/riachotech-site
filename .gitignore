# Backups gerados ao editar as páginas em produção.
# Continuam existindo no diretório servido (/var/www/riachotech) — só não entram no repo.
*.bak
*.bak-*

# Temporários de editor
*~
*.swp
*.swo
.*.swp
*.tmp
.DS_Store

# Dependências do build de CSS (S3) — site.css compilado É commitado,
# node_modules não. Ver README.md.
node_modules/

# Memória dos subagentes (Claude Code) — a skill em .claude/skills/ É versionada.
.claude/agent-memory/
